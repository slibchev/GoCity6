import 'package:flutter/material.dart';

class AppColors {
  // Основен тъмен City6 цвят
  static const Color primary = Color(0xFF081A2B);

  // Ярък синьо-циан акцент от логото
  static const Color secondary = Color(0xFF20C4FF);

  // Electric blue за по-силни акценти
  static const Color accent = Color(0xFF2F80FF);

  // Маршрут върху картата
  static const Color route = Color(0xFF2F80FF);

  // Леко синьо-сиво-метален фон
  static const Color background = Color(0xFFE3E6EA);

  // Светли панели и карти
  static const Color surface = Color(0xFFF8FAFC);

  // Тъмни панели / premium секции
  static const Color surfaceDark = Color(0xFF0D2438);

  // Основен текст
  static const Color textPrimary = Color(0xFF102033);

  // Вторичен текст
  static const Color textSecondary = Color(0xFF66788A);

  // Сребрист акцент от логото
  static const Color silver = Color(0xFFC9D2DC);

  // Леки граници
  static const Color border = Color(0xFFD9E2EC);

  // Статуси
  static const Color success = Color(0xFF22A06B);
  static const Color warning = Color(0xFFF2A93B);
  static const Color error = Color(0xFFD64545);
}
